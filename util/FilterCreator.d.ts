/**
 * Crates the filter needed to interpret input for the pagination
 * @returns {Function} The button filter
 */
export function FilterCreator({ message, interaction, buttonList, authorIndependent, selectMenu }: {
    message: any;
    interaction: any;
    buttonList: any;
    authorIndependent: any;
    selectMenu: any;
}): Function;
/**
 * Crates the filter needed to interpret input for the pagination
 * @returns {Function} The button filter
 */
export function FilterCreator({ message, interaction, buttonList, authorIndependent, selectMenu }: {
    message: any;
    interaction: any;
    buttonList: any;
    authorIndependent: any;
    selectMenu: any;
}): Function;
